#include <stdio.h>

#define SIZE 72

void seperate();

void seperate()
{

}

int main()
{
    int a[SIZE], b[5], c[5];    // 存放最终结果的数组
    int i, j;
    int n;
    scanf("%d", &n);
    for (i = n; i >= 1; i--, n--)
    {
        b[i] = n*(n-1);
    }
    for (i = 0; i < ; i++)
        printf("%d\n", a[i]);
}