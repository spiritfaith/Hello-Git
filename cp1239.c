#include <stdio.h>
// 编写函数，通过指针将一个字符串反向。
// 输入格式要求："%s"提示信息："Enter string:"      oAjBfZA
// 输出格式要求："a=%s\n"       Enter string:a=AZfBjAo
int main()
{
    char arr[100];
    int i;
    char tmp;
    for (i = 0; i < 100; i++)
    {
        scanf("%s", &tmp);
        expen[i] = tmp;
    }
    
    printf("Enter string:a=%s\n", )
}