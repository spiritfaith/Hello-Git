#include <stdio.h>

#define MAXDIGIT 100

int main()
{
    int arr1[MAXDIGIT] = {1, 2, 5}, arr2[MAXDIGIT] = {4, 3}, arr3[MAXDIGIT];
    int len1 = 3, len2 = 2, len3 = 0;
    int i, j;
    int temp[MAXDIGIT], tlen = 0;
    for (i = len2 - 1; i >= 0; i--)
    {
        for (j = len1 - 1; j >= 0; j--)
        {
            
        }
    }
}